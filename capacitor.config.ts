import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.welfaremaster.app',
  appName: '2026 기초연금 가이드',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
